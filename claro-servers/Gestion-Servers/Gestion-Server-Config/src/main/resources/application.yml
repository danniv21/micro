security:
  basic:
    enabled: false
management:
  security:
    enabled: false
server:
  port: 8080
spring: 
  cloud:
    config:
      server:
        git:
          uri: https://github.com/cruizg/conf
